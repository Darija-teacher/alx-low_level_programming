#include "main.h"

/**
 * print_time - Draws a straight line according to parametr
 * @n: The number of line to draw
 * Return: empty
 */

int print_line(int n)
{
	int x;

	if (n <= 0)
	{
		_putchar(('\n');
				}
				else
				{
				for (x = 0; x < n; x++)
				{
				_putchar(95);
				}
				_putchar('\n');
				}
				}
